{
  "name": "querosdev-site",
  "version": "1.0.0",
  "description": "Nikodem Kustusz Studio — portfolio site (Eleventy)",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "node -e \"require('fs').rmSync('_site',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
